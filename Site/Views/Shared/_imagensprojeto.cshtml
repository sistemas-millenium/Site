@model IEnumerable<Infra.Entities.ImagemProjeto>
@*<style>

    .carousel-item.principal {
    padding-left: 25%;
    }

    body {
    background-color: black;
    }

    @@media screen and (max-width: 475px) {

    .carousel-item.principal {
    padding-left: 33%;
    }

    button.btn.btn-primary {
    margin-bottom: 3%;
    }

    }
    </style>*@
<div class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog" id="modalimgs" aria-labelledby="myLargeModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div id="carouselExampleeControls" style="width:100%" class="carousel slide" data-ride="carousel">
                <div class="carousel-inner">
                    @foreach (var imagem in Model.OrderBy(x => x.Ordenacao))
                    {
                        if (imagem.ImagemProjetoId == Model.First().ImagemProjetoId)
                        {
                            <div class="carousel-item active">
                                <img class="d-block w-100" src="@Url.Action("ObterImagem","ImagensProjeto",new {filename = imagem.Caminho, projetoid = imagem.ProjetoId})">
                            </div>
                        }
                        else
                        {
                            <div class="carousel-item">
                                <img class="d-block w-100" src="@Url.Action("ObterImagem","ImagensProjeto",new {filename = imagem.Caminho, projetoid = imagem.ProjetoId})">
                            </div>
                        }
                    }
                </div>
                <a class="carousel-control-prev" href="#carouselExampleeControls" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleeControls" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                </a>
            </div>
        </div>
    </div>
</div>